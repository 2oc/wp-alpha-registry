apiVersion: v1
kind: BuildConfig
metadata:
  labels:
    app: proxy-server
  name: proxy-server
  namespace: weepee-registry
spec:
  output:
    to:
      kind: ImageStreamTag
      name: proxy-server:latest
  resources: {}
  source:
    git:
      uri: https://github.com/weepee-org/openshift-webproxy.git
      ref: release
    type: Git
  strategy:
    dockerStrategy: {}
    type: Docker
  triggers:
    - github:
        secret: IkuuJGu6vz/enC+w
      type: GitHub
    - generic:
        secret: QJTteWbCvb60B/nY
      type: Generic
    - type: ConfigChange
